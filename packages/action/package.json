{
  "type": "module",
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "lint": "npx eslint . && npx depcheck && tsc",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "esbuild": "0.19.5"
  }
}
