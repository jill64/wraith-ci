{
  "type": "module",
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "lint": "eslint . && tsc",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "@actions/exec": "1.1.1",
    "ts-retry": "4.2.5",
    "esbuild": "0.20.1",
    "@types/semver": "7.5.7",
    "semver": "7.6.0"
  }
}
