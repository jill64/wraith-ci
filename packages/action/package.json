{
  "type": "module",
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "lint": "eslint . && tsc",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@actions/exec": "1.1.1",
    "ts-retry": "4.2.3",
    "esbuild": "0.19.5",
    "twitter-api-v2": "1.15.2"
  }
}
