<h2 class="text-3xl font-bold">Ghost Bump</h2>
<p>
  Ghost Bumpはコミットのプッシュ時に<code>package.json</code>に記載された
  <code>version</code>を検出し、バージョンを上げるためのコミットを作成します。
</p>
<p>
  バージョンを上げる際はPRのタイトルを取得し、ルールに従ってバンプが実行されます。
</p>
