{
  "type": "module",
  "scripts": {
    "build": "npx wrangler deploy --dry-run",
    "deploy": "npx wrangler deploy",
    "lint": "eslint . && tsc",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/semver": "7.5.5",
    "semver": "7.5.4"
  }
}
