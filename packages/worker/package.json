{
  "type": "module",
  "scripts": {
    "build": "npx wrangler deploy --dry-run",
    "deploy": "npx wrangler deploy",
    "lint": "npx eslint . && tsc",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20241230.0",
    "@types/node": "22.10.4"
  }
}
