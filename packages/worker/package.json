{
  "type": "module",
  "scripts": {
    "build": "npx wrangler deploy --dry-run",
    "deploy": "npx wrangler deploy",
    "lint": "npx eslint . && tsc",
    "format": "npx prettier --write ."
  }
}
